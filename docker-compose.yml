version: '3'

services:
  frontend:

    image: 14790897/frontend-handwriting:latest
    ports:
      - "2345:80"
    depends_on: 
      - backend

  backend:

    image: 14790897/backend-handwriting:latest
    ports:
      - "5000:5000"
